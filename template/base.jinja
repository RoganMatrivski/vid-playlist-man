<!doctype html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>🚀</title>

    <!-- Tailwind config must be set before loading the CDN script -->
    <script>
        // Use 'media' so Tailwind's `dark:` variant follows prefers-color-scheme
        window.tailwind = { config: { darkMode: 'media' } }
    </script>

    <!-- Tailwind CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="min-h-screen bg-gray-50 dark:bg-gray-900 transition-colors duration-300">
    <main class="max-w-5xl mx-auto p-6">
        <header class="mb-8">
            <h1 class="text-3xl font-extrabold text-gray-900 dark:text-gray-100">
                {{ title }}
            </h1>
            <p class="mt-2 text-gray-600 dark:text-gray-300">
                {{ subtitle }}
            </p>
        </header>
        {% block content %}
        {% endblock %}
    </main>
</body>

</html>